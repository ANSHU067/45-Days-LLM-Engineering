"""
Exercise 2 — BMI Calculator (STUDENT STUB).

BMI = weight(kg) / height(m)^2
Categories:
    BMI < 18.5            -> Underweight
    18.5 <= BMI < 25      -> Normal
    25   <= BMI < 30      -> Overweight
    BMI >= 30            -> Obese

Run (asks for input):
    python bmi_calculator.py
"""

# TODO 1: ask for weight in kg and height in metres.
#         Remember input() returns a string -> cast with float()!
# weight = ...
# height = ...

# TODO 2: compute bmi = weight / (height ** 2)
#         (** is "to the power of"; height squared)

# TODO 3: use if / elif / elif / else to print the category.
#         Order your checks from lowest BMI to highest (or use the right boundaries).

# TODO 4: print the BMI rounded to 1 decimal with an f-string -> f"{bmi:.1f}"
